import { type Module } from "@divi/types";
import { type CampaignProgressAttrs } from "./types";

export const ModuleStyles = (
  _props: Module.StylesProps<CampaignProgressAttrs>,
) => {
  return null;
};
