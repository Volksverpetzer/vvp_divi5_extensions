import { type ModuleLibrary } from '@divi/types';
import { type AuthorProfileAttrs } from './types';

export const ModuleScriptData = (_props: ModuleLibrary.Module.ScriptData.CustomScriptDataProps<AuthorProfileAttrs>) => {
    return null;
};
