import { type ModuleLibrary } from '@divi/types';
import { type FactCheckSearchAttrs } from './types';

export const ModuleStyles = (props: ModuleLibrary.Module.Style.CustomStylesProps<FactCheckSearchAttrs>) => {
    return null;
};
