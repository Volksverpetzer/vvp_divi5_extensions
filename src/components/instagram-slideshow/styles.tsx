import { type ModuleLibrary } from '@divi/types';
import { type InstagramSlideshowAttrs } from './types';

export const ModuleStyles = (props: ModuleLibrary.Module.Style.CustomStylesProps<InstagramSlideshowAttrs>) => {
    // Custom dynamic styles can be added here if needed.
    return null;
};
