import { type ModuleLibrary } from '@divi/types';
import { type InstagramSlideshowAttrs } from './types';

export const ModuleScriptData = (props: ModuleLibrary.Module.ScriptData.CustomScriptDataProps<InstagramSlideshowAttrs>) => {
    return null;
};
