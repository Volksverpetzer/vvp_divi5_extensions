import { type ModuleLibrary } from "@divi/types";
import { type ContentOverviewAttrs } from "./types";

export const ModuleScriptData = (
  props: ModuleLibrary.Module.ScriptData.CustomScriptDataProps<ContentOverviewAttrs>,
) => {
  return null;
};
