import { type Module } from "@divi/types";
import { type ContentOverviewAttrs } from "./types";

export const ModuleStyles = (
  _props: Module.StylesProps<ContentOverviewAttrs>,
) => {
  return null;
};
