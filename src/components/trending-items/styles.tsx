import { type Module } from "@divi/types";
import { type TrendingItemsAttrs } from "./types";

export const ModuleStyles = (
  _props: Module.StylesProps<TrendingItemsAttrs>,
) => {
  return null;
};
